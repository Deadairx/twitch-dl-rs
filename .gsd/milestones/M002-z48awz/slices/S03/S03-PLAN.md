# S03: Intake Flexibility

**Goal:** Add queue-video <url> command that queues a single VOD by URL, inferring channel from Twitch API and merging into queues/<channel>.json. Make download-all channel arg optional.
**Demo:** After this: After this: run queue-video on a Twitch URL, then run download-all with no arguments and watch the queued item download.

## Tasks
