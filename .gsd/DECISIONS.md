# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | scope | Product framing | Reframe from a Twitch-only downloader into a queue-first, artifact-first media ingestion pipeline | The real workflow is media-to-transcript processing, not isolated downloading | No | agent |
| D002 | M001 | pattern | Core workflow model | Center the CLI on durable per-item artifact/job state | The user wants unattended progress, resume behavior, and clear failure visibility | No | agent |
| D003 | M001 | quality-attribute | Transcript default bar | Bias toward trustworthy transcript output over raw speed | Downstream notes and memory work depend on transcript trustworthiness | Yes — if a later hybrid strategy proves equally trustworthy | agent |
| D004 | M001 | convention | Cleanup behavior | Cleanup is explicit operator action via candidate review, not automatic deletion | Source deletion is destructive and should remain under manual control | No | agent |
| D005 | M001 | scope | Milestone split | M001 proves reliable intake-to-transcript; M002 adds notes and Ember; M003 adds source expansion and polish | This sequences trust-critical backbone work before downstream and expansion work | Yes | agent |
| D006 |  | architecture | How to handle discrepancy between task summaries and actual slice delivery for S01 | Document the gap honestly in slice summary; record what was actually delivered vs claimed; mark incomplete work as blocking for downstream slices | Task summaries for T01/T02/T03 claim complex lifecycle types (JobLifecycleState, StageLifecycleState, etc.), three regression tests, and a status CLI command. Actual implementation uses simple boolean flags, zero tests, and no status command. Writing a dishonest slice summary would hide critical gaps (missing status CLI and test coverage) from downstream agents and from the human planner. Documenting the truth in the summary allows S02 to proceed using the durable queue/status files while flagging that status CLI is a critical blocker for human UAT and milestone sign-off." | Yes | agent |
| D007 | M001/S02 | architecture | How to decouple download from transcription stages | Extract into composable async helpers and add batch CLI commands that read persisted queue/artifact state | Enables operators to queue work once, then download and transcribe at different times. Persistent status allows resumption on failures. | Yes | agent |
| D008 | M001/S02 | architecture | Error handling in batch processing commands | Implement --continue-on-error flag to allow partial progress; failed items logged and persisted in status.json | Allows long-running batch operations to continue despite transient failures. Users can inspect failures later and retry selectively. | Yes | agent |
| D009 | M001/S02 | requirements | R003 - Downloading must continue making progress even when transcription is pending | Validated by S02 implementation | download-all and transcribe-all commands decouple these stages; download can proceed independently via download-all while transcription remains pending | No | agent |
| D010 | M001/S02 | requirements | R012 - Interrupted or partial work must be resumable without redoing completed stages | Validated by S02 implementation | Batch commands filter by artifact status; download-all skips already-downloaded items; transcribe-all skips already-transcribed items. Status.json persists across invocations. | No | agent |
| D011 | M002-z48awz | convention | Single-video queue intake UX | Separate `queue-video <url>` command; infers channel from Twitch API, merges into queues/<channel>.json | Keeps channel-queue structure consistent so download-all picks up queue-video entries automatically. Avoids polluting `queue` with a different intake mode. | Yes | collaborative |
| D012 | M002-z48awz | convention | download-all channel argument optionality | Channel argument becomes an optional filter; omitting it walks all queue files and merges them | Operators should not need to remember channel names to drain the queue. Optional channel acts as a filter for targeted runs. | Yes | human |
| D013 | M002-z48awz | architecture | Display metadata schema ownership | Title, uploaded_at, and channel live in metadata.json only; status command reads both metadata.json and status.json per artifact for display | Keeps schemas clean and single-purpose. The extra per-artifact read is acceptable; duplicating display fields into status.json would create a sync burden. | Yes | human |
| D014 | M002-z48awz | convention | Queued-but-not-downloaded item visibility in status | Show queued items mixed in by default; no flag needed to see them | Operators want a single view of everything in flight. Hiding queued items behind a flag creates a mental model mismatch. | Yes | human |
| D015 | M002-z48awz/S01/T02 | architecture | Metadata fetch ordering in bare download path | GQL metadata fetch occurs after stream resolution but before prepare_artifact_dir | Ensures no orphan artifact directories are created if metadata fetch fails. Allows download to abort cleanly with error message and recovery hint (--skip-metadata) before any filesystem side effects. Preserves artifact directory invariant: if a directory exists, it has at least metadata.json and status.json. | Yes | agent |
| D016 | M002-z48awz/S01/T02 | architecture | Context threading pattern for metadata propagation | Pass vod_context as Option<(&str, &str, &str)> (borrowed str references); fetch returns owned Strings, convert to borrows for from_download | Avoids lifetime parameter cascades through the call stack (download_vod -> from_download -> various callers). Owned Strings from GQL fetch are temporary and can be safely borrowed within download_vod scope. Simpler to read and maintain than lifetime-parameterized closures or context structs. | Yes | agent |
| D017 | M002-z48awz/S01/T02 | architecture | Artifact normalization: bare download writes status.json | Both bare download and queue-backed download paths write status.json with downloaded=true after media download completes | Normalizes artifact structure across intake paths. Operators can run status command on artifacts from either path (download <url> or download-all after queue-video). S02 status display depends on this invariant. Removes a class of edge cases where status command would fail or produce confusing output. | Yes | agent |
